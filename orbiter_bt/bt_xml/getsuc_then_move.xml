<?xml version="1.0"?>
<root main_tree_to_execute="BehaviorTree">
    <!-- ////////// -->
    <BehaviorTree ID="BehaviorTree">
    
        <Sequence>
            <Action ID="getSucPose" itemName="default" pose="{pose}"/>
            <Action ID="MoveArm_CuRobo" arm_goal="{pose}" traj_type="0"/>
            <Action ID="MoveArm_CuRobo" arm_goal="{pose}" traj_type="1"/>
            <Action ID="MoveArm_CuRobo" arm_goal="{pose}" traj_type="6"/>
            <Action ID="suctionCmd" cmd="1"/>
            <Action ID="MoveArm_CuRobo" arm_goal="{pose}" traj_type="2"/>
            <Action ID="suctionCmd" cmd="0"/>
            <Action ID="waitUntilActivate"/>
            
        </Sequence>
    </BehaviorTree>
    <!-- ////////// -->
</root>
